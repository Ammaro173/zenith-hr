import Loader from "@/components/loader";

export default function LoadingMembershipCard() {
  return (
    <main className="flex min-h-svh items-center justify-center bg-background">
      <Loader />
    </main>
  );
}
